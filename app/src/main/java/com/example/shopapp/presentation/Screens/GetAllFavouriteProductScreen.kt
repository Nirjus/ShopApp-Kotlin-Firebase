package com.example.shopapp.presentation.Screens

import androidx.compose.runtime.Composable
import androidx.navigation.NavController

@Composable
fun GetAllFavouriteProductScreen(
    navController: NavController,

    ) {
    // Implement the UI for displaying all favourite products
    // Use the viewModel to fetch and display the data
    // Use navController for navigation if needed
}